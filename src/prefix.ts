export class Prefix {
    symbol: string;
    name: string;
    factor: number;

    constructor(symbol: string, name: string, factor: number) {
        this.symbol = symbol;
        this.name = name;
        this.factor = factor;
    }
}